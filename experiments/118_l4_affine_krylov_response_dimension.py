import json, os
from flint import arb, ctx
from fractions import Fraction as F
BITS=int(os.environ.get('ARB_PREC_BITS','160'))
if BITS<160: raise SystemExit('ARB_PREC_BITS must be at least 160')
ctx.prec=BITS
z=arb(0);o=arb(1);rt2=arb(2).sqrt()

def padd(A,B):
 C=dict(A)
 for k,v in B.items():C[k]=C.get(k,z)+v
 return C
def pscale(c,A):return {k:c*v for k,v in A.items()}
def pmul(A,B):
 C={}
 for e,u in A.items():
  for f,v in B.items():
   k=tuple(e[i]+f[i] for i in range(3));C[k]=C.get(k,z)+u*v
 return C
def pder(A,j):
 C={}
 for e,v in A.items():
  if e[j]:
   ee=list(e);c=ee[j];ee[j]-=1;ee=tuple(ee);C[ee]=C.get(ee,z)+c*v
 return C
def vadd(a,b):return tuple(padd(a[i],b[i]) for i in range(3))
def vscale(c,a):return tuple(pscale(c,a[i]) for i in range(3))
def cross(a,b):return (padd(pmul(a[1],b[2]),pscale(-1,pmul(a[2],b[1]))),padd(pmul(a[2],b[0]),pscale(-1,pmul(a[0],b[2]))),padd(pmul(a[0],b[1]),pscale(-1,pmul(a[1],b[0]))))
def curl(a):return (padd(pder(a[2],1),pscale(-1,pder(a[1],2))),padd(pder(a[0],2),pscale(-1,pder(a[2],0))),padd(pder(a[1],0),pscale(-1,pder(a[0],1))))
def directional(v,a):
 out=[]
 for i in range(3):
  q={}
  for j in range(3):q=padd(q,pmul(v[j],pder(a[i],j)))
  out.append(q)
 return tuple(out)
def mv(S,v):
 out=[]
 for i in range(3):
  q={}
  for j in range(3):q=padd(q,pscale(S[i][j],v[j]))
  out.append(q)
 return tuple(out)
def vdot(a,b):
 q={}
 for i in range(3):q=padd(q,pmul(a[i],b[i]))
 return q
def odddf(n):
 if n<=0:return 1
 q=1
 while n>0:q*=n;n-=2
 return q
def savgmono(e):
 a,b,c=e
 if a%2 or b%2 or c%2:return F(0)
 aa,bb,cc=a//2,b//2,c//2;N=aa+bb+cc
 return F(odddf(2*aa-1)*odddf(2*bb-1)*odddf(2*cc-1),odddf(2*N+1))
def savg(P):
 q=z
 for e,v in P.items():
  f=savgmono(e)
  if f:q+=v*arb(f.numerator)/f.denominator
 return q
def norm2v(v):return savg(vdot(v,v))

def sharp_null4(V):
 nx=cross(X,V);Q=[[z]*3 for _ in range(3)]
 for i in range(3):
  for j in range(3):Q[i][j]=arb(3)/2*savg(padd(pmul(X[i],nx[j]),pmul(nx[i],X[j])))
 Qn=[]
 for i in range(3):
  q={}
  for j in range(3):q=padd(q,pscale(Q[i][j],X[j]))
  Qn.append(q)
 prod=vscale(-arb(5)/3,tuple(pmul(r2,c) for c in cross(X,tuple(Qn))))
 return vadd(V,vscale(-1,prod))
def K(V):return sharp_null4(vadd(mv(S,V),vscale(-1,directional(Sx,V))))

mons=[]
for a in range(5):
 for b in range(5-a):
  c=4-a-b;mons.append((a,b,c))
coords=[(i,e) for i in range(3) for e in mons]
def flatten(V):return [V[i].get(e,z) for i,e in coords]
def certified_rank(columns):
 basis=[];pivots=[]
 for col in columns:
  w=list(col)
  for p,b in zip(pivots,basis):
   c=w[p]
   # Always eliminate the previously certified pivot coordinate.  Even a narrow interval containing zero
   # represents a possible small coefficient and must not be skipped, or later coordinates could create a false rank increase.
   for j in range(len(w)):w[j]-=c*b[j]
  pivot=None
  for j,x in enumerate(w):
   if not x.contains(0):pivot=j;break
  if pivot is not None:
   q=w[pivot]
   w=[x/q for x in w]
   basis.append(w);pivots.append(pivot)
 return len(basis),pivots

X=({(1,0,0):o},{(0,1,0):o},{(0,0,1):o});r2=padd(padd(pmul(X[0],X[0]),pmul(X[1],X[1])),pmul(X[2],X[2]))
c=arb(3)/(2*rt2);S=((o,z,c),(z,o,c),(c,c,-arb(2)))
Sx=[]
for i in range(3):
 q={}
 for j in range(3):q=padd(q,pscale(S[i][j],X[j]))
 Sx.append(q)
Sx=tuple(Sx);qx=vdot(X,Sx);u3=tuple(padd(pscale(-arb(5)/3,pmul(r2,Sx[i])),pscale(arb(2)/3,pmul(qx,X[i]))) for i in range(3))
omega=curl(u3);R4=vadd(directional(omega,u3),vscale(-1,directional(u3,omega)));V=sharp_null4(R4)
cols=[];rows=[]
for k in range(12):
 cols.append(flatten(V));rank,piv=certified_rank(cols)
 rows.append({'iterate_k':k,'Krylov_vector_mean_square':str(norm2v(V)),'certified_span_rank_after_including':rank,'pivot_count':len(piv)})
 V=K(V)
print(json.dumps({'arb_precision_bits':BITS,'status':'PASS','cases':len(rows),'max_certified_rank':max(r['certified_span_rank_after_including'] for r in rows),'interpretation':'Starting from the dynamically generated N4 direction, repeatedly apply the intrinsic affine null-response operator K=P_null L_S.  The resulting Krylov space is generated by the physics itself.  Coordinate monomials are used only to certify the invariant span dimension.  Stabilization at a finite rank means affine sharpness maintenance closes inside a finite degree-four response space; continued rank growth shows how many independent l=4 servo directions are actually required before nonlinear cross-order effects are considered.','rows':rows},indent=2,allow_nan=False))
