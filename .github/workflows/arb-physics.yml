name: Arb physics checks

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  precision-sweep:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        bits: [160, 256, 512]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install arbitrary-precision stack
        run: |
          python -m pip install --upgrade pip
          python -m pip install python-flint mpmath
      - name: Record package versions
        run: |
          python - <<'PY'
          import flint, mpmath, sys
          print(sys.version)
          print('python-flint', getattr(flint, '__version__', 'unknown'))
          print('mpmath', mpmath.__version__)
          PY
      - name: Run scale-cancellation sanity gate remotely
        env:
          ARB_PREC_BITS: ${{ matrix.bits }}
        run: python experiments/00_precision_and_scaling.py | tee result-${{ matrix.bits }}.json
      - uses: actions/upload-artifact@v4
        with:
          name: arb-result-${{ matrix.bits }}bit
          path: result-${{ matrix.bits }}.json
