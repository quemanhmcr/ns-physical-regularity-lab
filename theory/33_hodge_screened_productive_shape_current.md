# Hodge-screened productive shape current

## THINK — let the productive geometry generate its own strain test tensor

Take a directed productive triad

`a=xi_a`, `n=R/|R|`, `b=xi_b`.

Its intrinsic shape coordinates are

`alpha=a.n`,

`beta=b.n`,

`gamma=a.b`.

Let a common symmetric incompressible strain `S` act on all three unit directions.  Their normalized direction equations are

`adot=P_a S a`,

`ndot=P_n S n`,

`bdot=P_b S b`,

where `P_v=I-v tensor v`.

For `alpha`, direct differentiation gives

`alphadot`

`=2 a.S n-alpha(a.S a+n.S n)`.

This can be written as a tensor contraction

`alphadot=M_alpha:S`,

where

`M_alpha`

`=a tensor n+n tensor a`

` -alpha(a tensor a+n tensor n)`.

Likewise

`betadot=M_beta:S`,

`gammadot=M_gamma:S`.

Each `M_g` is symmetric and trace free.  No external test tensor has been chosen: the productive triad itself generates the tensor that asks how a physical strain changes its own geometry.

## Exact shape-tensor norm

For any two unit vectors `u,v` with `g=u.v`, define

`M_g=u tensor v+v tensor u-g(u tensor u+v tensor v)`.

Then exactly

`|M_g|_F^2=2(1-g^2)^2`.

This identity has an immediate physical meaning.  If `|g|` is near one, the two directions are nearly locked together; changing their dot product at a fixed rate requires a larger common strain because the shape sensitivity tensor itself collapses.

## Hodge projection

At the natural material midpoint `m=(X_a+X_b)/2`, use the actual Hodge microscope

`S_m=S_h(r)+S_v(r)`.

Because the shape current is linear in the common strain,

`gdot_common=M_g:S_h(r)+M_g:S_v(r)`.

The exact vortical Hodge strain is

`S_v(r)`

`=integral_0^r [1-(rho/r)^5] Q(rho) d rho/rho`.

Therefore

`gdot_v(r)`

`=integral_0^r [1-(rho/r)^5] [M_g:Q(rho)] d rho/rho`.

This is the key representation:

> **the vortical contribution to a productive Gram-coordinate current is itself a Hodge-screened signed vorticity transaction.**

The Hodge kernel is unchanged.  Only the STF transaction direction is now generated by the current productive geometry.

## Canonical exact calibration

For the tangent strain carrier,

`S_h(r)=(1-r^2/L^2)S`,

`S_v(r)=(r^2/L^2)S`,

`Q(rho)=(14/5)(rho/L)^2 S`.

Contracting with any `M_g` gives

`j_v=(r^2/L^2) M_g:S`.

The screened integral gives exactly the same value because

`(14/5) int_0^r [1-(rho/r)^5] (rho/L)^2 d rho/rho`

`=r^2/L^2`.

Thus the new reset-current transaction is not a formal analogy; it is literally the previous Hodge transaction projected by a shape-generated tensor.

## Projected harmonic occupancy floor

Let

`j_h=M_g:S_h(r)`.

Cauchy-Schwarz in the physical STF strain space gives

`j_h^2 <= |M_g|^2 |S_h|^2`.

Numerically, a tiny positive vortical energy remainder should be certified directly rather than by asking an interval for `E_h+E_v` to be strictly larger than the nearly identical parent `E_h`.

The exact Hodge harmonic energy floor is

`E(B_r)>=(2pi/15)|S_h|^2 r^5`.

Using

`|M_g|^2=2(1-g^2)^2`,

we obtain

`E(B_r)`

`>= (pi/15) [j_h^2/(1-g^2)^2] r^5`.

This is sharp.  Equality is attained by a pure linear harmonic strain

`S_h=(j_h/|M_g|^2) M_g`.

So it is the exact minimum harmonic kinetic occupancy required to drive the chosen physical Gram coordinate at rate `j_h` on radius `r`.

Equivalently, finite total kinetic energy `E0` gives

`|j_h(r)|`

`<= sqrt(15 E0/pi) (1-g^2) r^(-5/2)`.

This is not an abstract gradient norm.  It is a rate ceiling for a specific physical shape coordinate supplied by the exact harmonic Hodge mode.

## Midpoint source decomposition in Navier-Stokes

Write `A_m=grad u(m)=S_m+W_m` and use `W_m` as the common spin gauge.

Then the endpoint and bridge direction equations split into

- common midpoint symmetric strain `S_m`;
- endpoint-minus-midpoint velocity-gradient mismatch;
- bridge-average-minus-midpoint mismatch;
- viscous endpoint direction renewal.

Common `W_m` cancels exactly from every Gram coordinate.

After the Hodge split of `S_m`, every coordinate current has the exact physical ledger

`gdot=j_h(r)+j_v(r)+j_res`,

where

`j_h=M_g:S_h(r)`,

`j_v=M_g:S_v(r)` is the screened vorticity transaction above, and

`j_res` contains only endpoint/bridge inhomogeneity and viscosity relative to the material midpoint frame.

## PROMOTE

1. `M_alpha,M_beta,M_gamma` are intrinsic shape-generated STF reset tensors.
2. Common-strain Gram current and Hodge vorticity transaction live in the same STF tensor space.
3. Each vortical Gram-coordinate current has an exact Hodge-screened signed transaction representation.
4. Each harmonic Gram-coordinate current has a sharp `r^5` kinetic occupancy floor.
5. The material midpoint gives a natural common frame in which rigid spin disappears and only harmonic strain, vortical transaction, bridge inhomogeneity, and viscosity remain.

## Do not promote yet

- Do not sum the three coordinate currents with an arbitrary Euclidean metric and call that a universal cost.
- Do not infer irreversible expenditure from `j_v` alone; it is signed and can be renewed or cancelled.
- Do not claim the projected harmonic occupancy floor controls the vortical/residual branches.

The value of the representation is that it identifies exactly which physical source must be attacked next.
