# Swap-symmetric coupled Hodge feedback

The stationary capacity velocity is invariant under the physical reflection `x <-> y`.  This reflection has determinant `-1`, so polar and axial variables transform differently: velocity obeys `u(Rx)=R u(x)`, while vorticity obeys `omega(Rx)=det(R) R omega(x)=-R omega(x)`.  The complete Hodge/Euler construction is equivariant only when this polar/axial distinction is respected.

Therefore the nine-dimensional degree-six `T4` feedback representation is reducible by a symmetry that belongs to the actual base flow.

Remote module 133 constructs the **axial** reflection action on the complete vorticity `T4` feedback space from the physical fields themselves.  Its physically fixed subspace has dimension five and its anti-fixed subspace has dimension four.  Using the polar action on vorticity reverses these dimensions; that failed observer is retained conceptually as an autopsy.

A swap-symmetric feedback `y` produces a swap-symmetric lower response, a unique swap-symmetric degree-four compensation through invertible `K44`, and a swap-symmetric degree-six response through invertible `K66`.  Hence the coupled feedback map preserves the five-dimensional fixed subspace.

The numerical Newton candidate found from the symmetric sequential source lies in this fixed subspace.  Subsequent existence certification should therefore be performed in these five physical symmetry channels.  This does not rule out additional nonsymmetric coupled roots; it is an existence-oriented reduction, not a uniqueness theorem for the full nine-dimensional map.
