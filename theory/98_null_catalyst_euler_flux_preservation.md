# Euler conversion of null ancestry without circulation expenditure

For `B=diag(2,-1,-1)`,

`omega=A(2x,-y,-z)`

has tangent Hodge velocity

`u=(0,-A x z,A x y)`.

The vorticity-line ODE is

`x'=2Ax`, `y'=-Ay`, `z'=-Az`.

Hence

`x(y^2+z^2)=constant`

and the azimuth around the x-axis is fixed. The lines are through-going hyperbolic ancestry carriers, not closed loops inside a centered source ball.

The material velocity behaves very differently:

`u_x=0`,

`d(y^2+z^2)/dt=0`.

Fluid particles rotate around the x-axis at fixed `x` and fixed radius. Every centered sphere is material for this instantaneous flow.

Now evaluate the self-Euler vorticity source. Exactly,

`G=(omega.grad)u-(u.grad)omega=2A u`.

Therefore

`G.n=0`

on every centered sphere.

So the nonlinear conversion that creates productive `Q` does not change the radial vorticity-flux density through those spheres at first Euler order. This is a concrete geometric realization of Kelvin ancestry conservation: Euler dynamics can reorganize a fixed circulation ancestry into productive strain without mutating its circulation.

The polynomial catalyst is not finite-energy globally. Thus the next unavoidable physical event is its spatial turnover/localization. Any actual finite-energy realization must bend/close the through-going flux and destroy the exact linear viscous zero mode somewhere. That turnover collar is where a genuine viscous/ancestry current can enter.
