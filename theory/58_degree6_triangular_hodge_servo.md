# Degree-six triangular Hodge servo

## THINK — finish the first higher-order maintenance step completely

The unique degree-four Hodge servo cancels the first nonlinear null response, but its actual Hodge velocity generates a nonzero degree-six null field.  Before proposing any larger Navier-Stokes mechanism, the next layer must be exhausted with the same physical discipline used at degree four.

The homogeneous degree-six divergence-free vorticity space has dimension

`3*C(8,2)-C(7,2)=63`.

Its productive transaction sector is again the five-dimensional toroidal `l=2` sector.  Therefore the complete degree-six transaction-null space has dimension `58` and intrinsic surface-Hodge decomposition

`N_6 = P_1 + P_3 + P_5 + P_7 + T_4 + T_6`,

with dimensions

`3+7+11+15+9+13=58`.

No smaller modal ansatz is imposed.

## Natural Hodge velocities

Every poloidal sector is generated as the curl of a tangent toroidal degree-seven velocity.

For toroidal `l=4,6` vorticity, use the exact general Hodge lift already calibrated in module 125.  A degree-six toroidal field has a degree-seven vortical velocity plus an unavoidable lower harmonic companion:

- `T_4` carries a harmonic degree-three companion;
- `T_6` carries a harmonic degree-five companion.

This makes the linear maintenance hierarchy triangular rather than block-diagonal.

## Degree-six diagonal block

Define `K_66` as the degree-six null response produced by a degree-six control under the base stationary carrier:

`K_66 V = P_null,6([V,u_1] + [omega_2,U_low,5])`.

The second term is present only for the `T_6` sector, whose harmonic companion has degree five and therefore acts on `omega_2` at degree six.

Module 129 measures `rank K_66` on the complete 58-dimensional null space and, when the block is invertible, solves

`K_66 V_6 = -N_6`,

where `N_6` is the exact degree-six null field generated by the validated unique degree-four Hodge servo.

The sphere norm `||V_6||^2` is actual vorticity content, not an abstract control norm.

## Lower triangular backreaction

The `T_4` part of a degree-six control has a harmonic degree-three velocity companion.  Acting on the base `omega_2`, it produces a degree-four null response

`K_46 V_6 = P_null,4 [omega_2,U_low,3]`.

Module 130 measures the rank of this complete off-diagonal block and evaluates it on the unique degree-six diagonal servo.

If `K_66` is invertible and `K_46 V_6 != 0`, then a sequential strategy

`cancel degree 4 -> cancel degree 6`

cannot preserve the earlier degree-four cancellation: the unique degree-six servo reopens the lower response.

This is a physical triangular-coupling statement.  It is **not** yet a proof that no coupled nonlinear pair `(V_4,V_6)` can solve both levels simultaneously, because changing `V_4` also changes the nonlinear degree-six source.

## Observer discipline

The general Hodge-lift tangency check is certified by the structural source-sphere identity

`l A+B+l C=0`,

not by requiring mixed-homogeneity velocity fields to be tangent on every radius.  Raw sphere cancellation is retained only as an autopsy.

## PROMOTE / KILL criteria

PROMOTE if remote 160/256/512 audit survives:

1. the complete degree-six null Hodge space has dimension 58;
2. the full diagonal maintenance rank/nullity is known without a truncated ansatz;
3. if invertible, the degree-six cancelling servo is unique and has a measurable physical enstrophy;
4. the lower triangular `K_46` backreaction is measured intrinsically.

KILL / DO NOT CLAIM:

1. do not infer an infinite hierarchy merely from one nonzero backreaction;
2. do not infer a global NS obstruction from the linear triangular system alone;
3. do not identify polynomial homogeneity itself with a nonreusable physical resource.

This layer only closes the unfinished degree-six question before any larger PDE proposal is made.
