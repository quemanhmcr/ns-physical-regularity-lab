# Critical frozen winding escape at the Hodge energy horizon

## THINK — unbounded source transaction need not aggregate new circulation

The cumulative source-Reynolds analysis showed that a frozen infinite cascade needs `Gamma_Q=sL^2` to become unbounded.  For a closed extremal source this translated into unbounded circulation-flux aggregation.

The through-going slow-spiral geometry exposes a stronger escape: the **same frozen circulation lineage can be reused by winding through the source many times**.

Consider the divergence-free spherical-shell calibration

`omega_r=A sin^2(theta)cos(theta)/r^2`,

`omega_phi=B sin(theta)cos(theta)/r`.

The tangential part is exactly the sharp axisymmetric angular carrier; the radial part is transaction-null.

Its positive through-going circulation flux is

`Gamma=pi A/2`.

At `theta=pi/4`, traversing a shell of thickness `Delta r` in `N` turns requires

`B/A=pi N/Delta r`.

The characteristic circulation-dimensional Hodge transaction is

`Gamma_Q=2Br/5`,

so

`Gamma_Q/Gamma=(4/5) N r/Delta r`.

Thus winding multiplicity replaces ancestry-flux aggregation.

## Exact winding/null duality

The full-shell tangential enstrophy is exactly the sharp projector floor,

`Z_sharp=(8pi/15)B^2 Delta r`.

The radial transaction-null enstrophy is

`Z_null=(32pi/105)A^2 Delta r/(R1R2)`.

At the characteristic radius `r=sqrt(R1R2)`,

`(Z_null/Z_sharp) (Gamma_Q/Gamma)^2 = 64/(175pi^2)`.

So making the lineage closer to the sharp tangent carrier by reducing its radial null fraction **increases** the amount of transaction extracted from the same circulation.  Near-sharpness is not a topological cost here; it is the high-winding reuse limit.

This demotes the earlier interpretation

`unbounded Gamma_Q => unbounded independent material circulation stock`.

That statement is valid for the closed extremal Clebsch-cell representation but not for general through-going ancestry.  A fixed material circulation can be reused through increasing oriented winding, exactly as the original lineage transaction formula suggested.

## Critical frozen conveyor

Now combine three intrinsic kinematic laws in a canonical escape calibration.

Let `n` be the material line-stretch/winding factor.

1. Frozen vortex-line stretching gives

   `n~e^N`, where `dN=s dt` is strain count.

2. Shape-locked pair ancestry gives bridge/source contraction

   `L=L0 n^-2`.

3. Winding transaction from fixed circulation `Gamma` gives

   `s=C Gamma n/L^2`,

   with `C` an order-one geometry coefficient.

Therefore

`s=(C Gamma/L0^2)n^5 = k n^5`,

and because `n'/n=s`,

`n'=k n^6`.

This closes to the finite-time solution

`n=[5k(T-t)]^-1/5`,

`L=L0[5k(T-t)]^(2/5)`,

`s=1/[5(T-t)]`.

The exponent `2/5` of the Hodge finite-energy horizon has reappeared from an independent material mechanism: one winding/stretch degree plus the square of the `n^-2` source contraction in the strain kernel produces the fifth power `n^5`.

## Scalar-budget autopsy

For this critical branch,

`s^2L^5=constant`.

The sharp productive enstrophy behaves like

`s^2L^3~(T-t)^(-4/5)`,

so its spacetime integral is finite.

The localized viscous exposure

`integral nu/L^2 dt`

also has a finite tail `~(T-t)^(1/5)`.

Meanwhile

`Re_source=sL^2/nu~(T-t)^(-1/5)->infinity`.

Thus the branch simultaneously:

- saturates finite kinetic-energy scaling;
- respects finite viscous enstrophy dissipation;
- becomes increasingly frozen at the source scale;
- drives unbounded transaction through winding of fixed circulation.

This is not an exact Navier-Stokes blow-up solution.  It is a self-consistent physical escape mechanism assembled from independently derived transaction, pair-cell, Hodge, and frozen-lineage laws.  Its role is adversarial: any claimed regularity mechanism that does not break this geometry is incomplete.

## PROMOTE / KILL

PROMOTE:
1. Through-going winding can reuse fixed circulation to generate unbounded `Gamma_Q`.
2. The slow-spiral calibration has an exact inverse-square null/winding-gain duality.
3. Combining frozen line stretch, pair bridge contraction, and winding transaction produces the critical `L~tau^(2/5)` conveyor.
4. This critical branch survives every scalar finite-energy/enstrophy/viscous-exposure ledger currently established.

KILL / DEMOTE:
1. Unbounded `Gamma_Q` as universal proof of unbounded independent circulation ancestry stock.
2. Transaction-null enstrophy as a monotone penalty against through-going winding.
3. Any strategy that expects a stronger scalar energy estimate alone to eliminate the high-Re branch.

## Next frontier — can a directed network sustain the winding conveyor?

The sharp axisymmetric donor used in the calibration produces a target strain but does not by itself explain who continually stretches and winds that donor.  Sustained growth therefore returns to the directed transaction network.

The remaining question is now highly specific:

**can a finite closed network of frozen high-circulation lineages mutually generate the critical winding law indefinitely while keeping every edge in the productive Gram chamber and avoiding ancestry-current renewal?**

This reconnects the high-Re escape to the positive-cycle/pair-cell hierarchy, but now with the correct physical reuse variable: winding of material circulation, not circulation amount.
