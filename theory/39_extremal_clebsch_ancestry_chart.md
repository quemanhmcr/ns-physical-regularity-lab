# Extremal productive carrier as a Clebsch ancestry chart

## THINK — does compulsory inward cascade consume a fixed amount of circulation at every scale?

The sharp projector identifies the least-vorticity carrier of a prescribed transaction tensor.  To reconnect it to material ancestry, first ask what circulation flux that extremal geometry actually carries, without assuming a filament model.

Consider a fixed STF transaction ray

`Q(r)=q(r) E`.

The sharp minimum productive vorticity is

`omega_prod=-(5/3) q(r) n cross E n`.

Define

`A'(r)=(5/3) q(r)/r`,

`B(x)=-(1/2) x.E.x`.

Then

`grad A=A'(r)n`, `grad B=-E x`,

and exactly

`omega_prod=grad A cross grad B`.

Thus the extremal carrier has Euler/Clebsch potentials generated by its own radial transaction amplitude and quadratic angular transaction geometry.

## Flux, not an individual Clebsch coordinate, is physical

The vorticity flux 2-form of `grad A cross grad B` is

`dA wedge dB`.

A cell bounded by two `A` surfaces and two `B` surfaces therefore carries circulation flux

`Delta Gamma=Delta A Delta B`.

The potentials themselves are not unique.  The canonical rescaling

`A -> c A`, `B -> B/c`

changes `Delta A` by an arbitrary factor but leaves `dA wedge dB`, and hence the physical vorticity field and circulation flux, unchanged.

Therefore `Delta A` alone is not a universal ancestry amount.  The physical object is the flux 2-form / flux cell.

## Transaction is radial Clebsch increment in the geometric gauge

For the geometrically selected gauge `B=-(1/2)x.E.x`,

`E:Q=q |E|^2`.

Hence over a radial interval

`J_E=integral (E:Q) dr/r`

`=(3/5)|E|^2 Delta A`.

So the transaction microscope is directly reading radial movement through the Clebsch flux chart, but only after the angular gauge has been fixed by the physical tensor `E`.

## ATTACK — fixed circulation per scale is false

Take `E=diag(2,-1,-1)`, so `|E|^2=6`.  On a sphere of radius `r`, the full range of

`B=-(1/2)x.E.x`

is

`Delta B=(3/2)r^2`.

A radial cell carrying transaction `J` has

`Delta A=5J/18`.

Therefore its full angular circulation-dimensional flux is

`Delta Gamma=(5/12)J r^2`.

At fixed transaction strength, the required circulation amount tends to zero like `r^2` as the physical scale shrinks.

This kills the naive route

`infinite number of inward scales => infinite total circulation amount`.

Nature can geometrically amplify a smaller circulation flux at a smaller radius into the same strain transaction.

## AUTOPSY

- Killed: a fixed positive circulation toll per productive scale.
- Killed: an individual Clebsch potential increment as gauge-invariant ancestry stock.
- Promoted: the sharp carrier has an intrinsic Clebsch flux geometry on every fixed transaction ray.
- Promoted: transaction strength per physical circulation scales like `r^-2`, explaining why shrinking scale is a genuine escape from static circulation budgets.

## Next frontier

The missing cost cannot be the amount of circulation alone.  It must concern how the flux geometry is transported or regenerated while its physical support scale shrinks.  The next observer is therefore the radial persistence operator of the minimum productive carrier under viscosity.
