# Sharp Hodge transaction carrier: the tangent strain carrier is the extremizer

## THINK — how much angular vorticity is genuinely required by a transaction tensor?

The Hodge shell tensor is

`Q=(3/(8pi)) integral [n tensor v+v tensor n] dOmega`,

where

`v=n cross omega`

is tangential to the sphere.

Large vorticity can contain arbitrarily much nonproductive content.  The physical question is therefore not to bound `Q` by a gross vorticity norm and stop.  It is to identify the **minimum tangential vorticity content whose signed angular organization can realize the prescribed transaction tensor Q**.

This is an inverse problem defined by the transaction itself.

## Exact transaction adjoint

For every symmetric trace-free test tensor `E`,

`E:Q=(3/(4pi)) integral (E n).v dOmega`.

Since `v.n=0`, only the tangential projection matters:

`E:Q=(3/(4pi)) integral [(I-nn)E n].v dOmega`.

Thus the transaction map has the physical adjoint field

`T^*E=(3/(4pi))(I-nn)E n`.

Sphere isotropy gives for STF `E`

`integral |E n|^2 dOmega=(4pi/3)|E|^2`,

`integral (n.E.n)^2 dOmega=(8pi/15)|E|^2`,

so

`integral |(I-nn)E n|^2 dOmega=(4pi/5)|E|^2`.

## Sharp angular transaction-enstrophy theorem

Set `E=Q`.  Then

`|Q|^2=(3/(4pi)) integral [(I-nn)Qn].v dOmega`.

Cauchy-Schwarz and the isotropic identity give

`|Q|^4`

`<= (9/(16pi^2)) (4pi/5)|Q|^2 integral |v|^2 dOmega`.

Therefore

`integral |n cross omega|^2 dOmega >= (20pi/9)|Q|^2`.

This constant is sharp.

Equality requires

`v=(5/3)(I-nn)Qn`.

Equivalently the minimum tangential vorticity itself is

`omega_t=-(5/3)n cross Qn`.

Radial vorticity is invisible to `Q` and only adds full enstrophy.

## Exact productive/null decomposition

The equality field is the minimum-norm preimage of `Q`.  Any tangential transaction carrier can be written

`v=v_Q+v_0`,

where

`v_Q=(5/3)(I-nn)Qn`

and

`T(v_0)=0`.

Because `v_Q` lies in the range of the adjoint while `v_0` lies in the transaction kernel, they are orthogonal in `L^2(S^2)`.  Hence the inequality sharpens to the exact Pythagorean identity

`integral |v|^2 dOmega`

`=(20pi/9)|Q|^2 + integral |v_0|^2 dOmega`.

This separates **productive transaction vorticity** from transaction-null angular vorticity without a Fourier decomposition and without an arbitrary coherence measure.

Rigid constant-vorticity backgrounds are examples of the null sector: they change `v` but contribute zero `Q`.

## The old tangent carrier is exactly the extremizer

For the self-contained tangent strain carrier,

`Q(rho)=(14/5)(rho/L)^2 S`,

while its tangential vorticity is

`omega_t=-(14/3)(rho/L)^2 n cross S n`.

But

`-(5/3)n cross Qn`

`=-(14/3)(rho/L)^2 n cross S n`.

Thus the canonical carrier used by the Hodge microscope is **exactly the sharp minimum-angular-enstrophy carrier of its transaction tensor on every sphere**.

It was not merely a convenient polynomial ansatz.

## Intrinsic closure geometry of the extremizer

Let

`f(n)=n.Q.n`.

Its surface gradient is

`grad_S f=2(I-nn)Qn`.

Therefore

`omega_t=-(5/6)n cross grad_S f`.

The extremal vorticity is tangent to the level sets of `f`, and its surface divergence vanishes structurally because it is the ninety-degree rotation of a surface gradient.

So the minimum transaction carrier comes with its own closed level-set geometry on the sphere.  Closure is generated by the quadratic transaction potential itself rather than by imposing a filament completion after the fact.

## Capacity pure-production specialization

At the optimal productive pair geometry, the unique shape-locked equal-amplification strain ray is

`S_* = sigma S_unit`,

with

`|S_unit|^2=21/2`.

A shell tensor producing only the two equal magnitude channels and zero shape channels has

`Q=q S_unit`.

Therefore the sharp theorem becomes

`integral |n cross omega|^2 dOmega >= (70pi/3) q^2`.

This is the minimum angular vorticity content of the pure-production transaction ray selected by the productive pair itself.

## Canonical volume calibration

For the tangent carrier in a ball of radius `L`, exact integration gives

`E_c=(8pi/405)|S|^2 L^5`,

while the pure harmonic linear strain with the same center tensor has

`E_h=(2pi/15)|S|^2 L^5`.

Hence

`E_c/E_h=4/27`.

The tangent carrier enstrophy is

`integral_B |omega|^2 dx=(112pi/45)|S|^2 L^3`,

which exactly equals the radial integral of the sharp shell floor.

The coefficient `4/27` is numerically identical to the independent maximum productive-pair product.  Record this as a coefficient resonance only.  The current theory does not establish a causal identity between the two extremal problems.

## AUTOPSY

### gross angular vorticity as productive currency

Killed.  Every carrier splits orthogonally into the unique minimum productive representative plus a transaction-null sector.

### tangent strain carrier as an arbitrary canonical family

Killed.  On each sphere it is the exact minimum-L2 tangential-vorticity representative of its `Q`.

### vortex-line closure must be imposed externally

Killed for the angular extremizer.  Its vorticity follows level sets of the transaction quadratic form.

### sharp angular floor alone proves regularity

Do not promote.  The source radius may shrink, transaction profiles may move radially, and material lineages may be replaced.  The theorem identifies the cheapest instantaneous angular carrier; it does not yet prove that this carrier cannot be regenerated indefinitely.

## PROMOTE

1. `integral |n cross omega|^2 >= (20pi/9)|Q|^2` is a sharp universal shell theorem.
2. The unique minimum productive carrier is `v_Q=(5/3)(I-nn)Qn`.
3. Every tangential vorticity field decomposes orthogonally into productive `Q` content plus a transaction-null sector.
4. The tangent Hodge strain carrier exactly saturates the theorem on every sphere.
5. The extremizer has intrinsic divergence-free quadratic-level-set closure geometry.
6. The capacity pure-production ray has the sharp specialization `(70pi/3)q^2`.

## Next frontier — ancestry of the minimum productive carrier

The instantaneous problem is now stripped of observer waste.  For a prescribed five-channel transaction tensor, we know the exact least angular vorticity geometry capable of carrying it.

The remaining regularity question is temporal:

**can a finite material ancestry stock repeatedly regenerate the minimum productive carrier at shrinking Hodge scales while keeping the pair near the productive chamber and avoiding viscous replacement?**

That is the natural point to reconnect the sharp transaction projection to the material-spacetime ancestry current.
