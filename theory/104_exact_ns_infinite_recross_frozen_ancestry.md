# Infinite source recrossing by one frozen ancestry element

The forced-recruitment ledger produces an infinite cumulative positive source-relative circulation crossing. That still does not prove infinite **distinct** ancestry.

The exact quadratic Navier-Stokes family

`u=(a(t)x+eps yz,-a(t)y,0)`

provides a direct attack. Its vorticity is

`omega=(0,eps y,-eps z)`.

Both `Delta u` and `curl omega` vanish, so the Kelvin viscous current is exactly zero. For arbitrary positive `lambda(t)`, choose

`a=-d log(lambda)/dt`.

On the material y-axis (`x=z=0`),

`y(t)=lambda(t)y0`.

Now let

`tau=1-t`,

`phi=t/(1-t)`,

`lambda=tau^(4/5)[1+sin^2 phi]`,

and choose the moving source radius

`R=tau^(4/5)R0`.

For `y0=(2/3)R0`,

`|y|/R=(2/3)[1+sin^2 phi]`.

Therefore the same material lineage crosses the shrinking source boundary at

`phi=k pi+pi/4`

and

`phi=k pi+3pi/4`

for every integer `k`. Infinitely many alternating crossings occur before `t=1`.

The circulation of an infinitesimal material flux tube around this lineage is unchanged at every crossing because the Kelvin current is zero.

The recrossing is also compatible with the local finite-energy Hodge scaling. At the crossing phases

`|a|~(2/3)tau^-2`,

while

`R~tau^(4/5)`,

so

`a^2R^5 ->4/9`.

Thus the exact `4/5` source shrinkage can support infinitely accelerated recrossing without forcing local harmonic occupancy to diverge.

## KILL

The implication

`infinite cumulative source-relative crossing => infinite distinct ancestry / irreversible renewal`

is false.

The same frozen material circulation element can be counted infinitely often by a shrinking non-material source.

## What survives

The critical null-catalyst branch requires more than infinite cumulative crossing: its **instantaneous** active cap circulation diverges. Sequential recrossing of one fixed flux element does not increase the simultaneous circulation inventory.

The next physical object is therefore fixed-time ancestry multiplicity/packing: can the same frozen lineage pierce the active cap many times simultaneously, or does the near-linear null-catalyst geometry force each positive cap crossing to represent a distinct flux element?
