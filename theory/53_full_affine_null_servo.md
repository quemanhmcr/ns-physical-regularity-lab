# Full affine null servo in the physics-generated six-dimensional response space

## THINK — give the surviving mechanism all affine freedom it actually generates

The one-direction `l=4` servo failed, but the intrinsic Krylov sequence

`N_4, K N_4, K^2 N_4, ...`,

`K=P_null L_S`,

stabilizes at rank six.  Therefore affine maintenance is not an infinite hierarchy.  The correct adversarial test is to allow all six directions.

## Intrinsic finite-dimensional solve

Let

`B_j=K^j N_4`, `j=0,...,5`.

The next iterate satisfies an exact closure relation

`B_6=sum_j a_j B_j`.

This determines the action of `K` on the physics-generated cyclic space.  Solve

`K V_servo=-N_4`

inside this six-dimensional space.

The coefficient chart used by the remote microscope is only a rank/linear-algebra certificate.  The actual basis vectors are the physical Krylov responses themselves.

## Interpretation

If no solution exists, the critical winding mechanism cannot even cancel its first generated null field while preserving the stationary affine environment.

If a solution exists, **promote the escape rather than hiding it**: affine sharpness maintenance is possible with finitely many response channels.  Then construct a divergence-free velocity with curl `V_servo` and test the nonlinear cross-interaction with the base localized carrier.

That nonlinear step is where a genuinely new angular degree may appear.
