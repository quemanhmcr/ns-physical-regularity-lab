# Critical high-Re reuse: closure/return geometry does not restore a packet toll

Module 188 leaves a deliberately sharp objection: the productive passes belong to one continuous divergence-free lineage, so they must be connected.  Perhaps the omitted returns carry the missing nonreusable cost.

Attack both closure extremes allowed by the physical geometry.

Use

`epsilon=rho^5`,

`R=K epsilon^(4/5)=K rho^4`,

`s=mu nu/epsilon^2`,

`Gamma=Re_Gamma nu`,

and the productive winding law

`s=Cw Gamma N/R^2`.

Saturated passage packing gives

`a=R/sqrt(N)=sqrt(Cw Re_Gamma/mu) epsilon`.

The closure microscope is only defined on the actual multi-pass branch `N>1`.  **AUTOPSY of Frontier run 31477455263:** the first version sampled coarse parameter points with `N<1` but still demanded `N` remote returns and a fixed-aspect tubular collar.  For one such point `N≈0.3`, the declared collar had `b>R_c`, so 160/256/512 all failed the same geometry-validity assertion.  This was a domain-of-model false red, not a precision instability and not evidence for a closure obstruction.  The corrected experiment skips non-multipass points rather than loosening any inequality.

Connect the `N` productive passes by `N` semicircular return bends of radius `R_c`.
For such bends,

`L_bend=N pi R_c`,

`K2=int kappa^2 ds=N pi/R_c`.

The directional viscous curvature term is therefore

`Tax_dir=nu Gamma K2`.

For a circulation-isolation collar `b=beta a` carrying at least fraction `theta` of `Gamma`, the tubular Cauchy floor is

`E_collar >= theta^2 Gamma^2 N R_c/4 * (1-b/R_c) log beta`,

provided `b<R_c`.

The relevant persistence observer is not raw `K2`, which can diverge on a shrinking object, but its length-averaged diffusion exposure over one maintenance time `tau=1/s`:

`Xi = nu tau K2/L_bend = nu tau/R_c^2`.

## ATTACK A — near-contact hairpins

Take

`R_c=chi a`, `chi>beta`.

Then exactly

`Xi_near = 1/(chi^2 Cw Re_Gamma)`.

The scale disappears.  A fixed high circulation Reynolds number keeps the tight return bends on the same favorable side of diffusion at every shrinking stage.

The geometric inventories are

`L_bend,near ~ epsilon^(3/5)`,

`Tax_dir tau ~ epsilon^(3/5)`.

Thus total curvature-square diverges, but the curvature tax accumulated on the shrinking maintenance clock vanishes.

## ATTACK B — remote returns

Take

`R_c=eta R`.

Then

`Xi_remote = epsilon^(2/5)/(mu eta^2 K^2) ->0`,

while

`L_bend,remote ~ epsilon^(2/5)`,

`Tax_dir tau ~ epsilon^(4/5)`.

Remote return geometry is even less exposed to curvature diffusion on the maintenance clock.

## AUTOPSY

A tempting closure repair was:

> one lineage reused `N->infinity` times must pay either a near-contact curvature catastrophe or an expensive remote closure.

That implication is false at the critical exponents currently forced by the source ledger.

Near-contact returns have a fixed high-Re persistence buffer.  Remote returns have a buffer that improves inward.  The local circulation-collar floor remains positive, but its physical support length shrinks fast enough that it does not create a non-summable circulation packet toll.

## KILL / SURVIVE

KILL, at this scope:

1. closure curvature alone repairs the distinct-packet theorem;
2. a fixed-aspect local circulation collar plus curvature produces a scale-divergent one-event ancestry cost;
3. choosing between near-contact and remote return geometries forces a scalar contradiction.

SURVIVES:

- global embedding/self-avoidance of the whole tube;
- collective Biot-Savart induction of productive and return branches;
- dynamic creation and preservation of the return/winding geometry;
- the shape-servo and transaction-null cancellation currents already exposed by the Hodge carrier modules.

So the frontier remains actor-of-actor ancestry, not circulation stock: **what actual material/network dynamics continually manufactures the geometry that lets the same high-Re circulation be reused?**
