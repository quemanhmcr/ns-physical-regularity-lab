# Sharp productive-carrier manifold is not dynamically invariant

## THINK — static near-sharpness is not enough

The slow-spiral winding escape can make the instantaneous transaction-null fraction arbitrarily small while reusing the same circulation many times.  This leaves a crucial dynamical question:

**if the vorticity is exactly on the minimum productive Hodge carrier at one instant, does its own Navier-Stokes/Euler nonlinearity keep it on that sharp manifold?**

If not, near-sharp winding requires active cancellation by the surrounding network rather than being a free persistent state.

## Exact test carrier

Use the symmetric capacity stationary-lock tensor with unit gain scale,

`S=[[1,0,3/(2sqrt(2))],`

`   [0,1,3/(2sqrt(2))],`

`   [3/(2sqrt(2)),3/(2sqrt(2)),-2]]`.

Its self-contained tangent Hodge carrier is

`u=(1-5r^2/3)Sx+(2/3)(x.S.x)x`,

`omega=-(14/3)x cross Sx`.

The vorticity is exactly the sharp minimum transaction carrier shell-by-shell.

Because `Delta omega=0`, its instantaneous viscous contribution vanishes in the interior.  The self-generated Euler vorticity derivative is therefore

`R_NL=(omega.grad)u-(u.grad)omega`.

This is computed exactly as a polynomial; no time stepping is used.

## Project the nonlinear residual with the same physical transaction projector

At each physical sphere, compute the transaction tensor `Q_R` of `R_NL`.  The unique minimum productive vorticity having that transaction is

`R_prod=-(5/3)n cross Q_R n`.

Define

`R_null=R_NL-R_prod`.

The sharp projector gives exactly

`<R_null,R_prod>_{S^2}=0`,

`<|R_prod|^2>=(5/9)|Q_R|^2`,

and

`<|R_NL|^2>=<|R_prod|^2>+<|R_null|^2>`.

The remote microscope tests these identities from exact polynomial sphere moments.

The key question is whether `R_null` vanishes.  It does not away from the center.  The cubic part of the tangent carrier interacts with its quadratic vorticity and generates higher angular/radial structure outside the degree-two minimum transaction sector.

## Physical meaning

The sharp projector defines the least-vorticity state for a given instantaneous strain transaction, but that state is **not a nonlinear invariant manifold**.

Therefore the critical winding branch has an additional requirement:

- winding can make the radial transaction-null component arbitrarily small at one instant;
- but the carrier's own nonlinear evolution regenerates transaction-null modes at an order-one rate measured in strain-time units;
- keeping the source near the sharp manifold requires other flow components to cancel, export, or continually reproject this null production.

This is different from a static null-enstrophy toll.  The cost is a **dynamic sharpness-maintenance current**.

## AUTOPSY

### Near-sharp instantaneous geometry implies persistent near-sharp dynamics

Killed.  The exact equality carrier immediately leaves the sharp manifold under its own nonlinear vorticity dynamics.

### Viscosity is responsible for that departure

Killed for the interior tangent carrier calibration: `Delta omega=0` exactly.  The first departure is nonlinear.

### This proves the winding escape impossible

No.  A surrounding transaction network may supply precisely the null-canceling/shape-servo fields needed to keep the core near sharp.  The result instead makes that network burden explicit.

## PROMOTE if remote polynomial audit survives

1. The minimum Hodge transaction carrier is an instantaneous orthogonal projection, not an invariant dynamics.
2. The stationary-lock tangent carrier generates a nonzero transaction-null nonlinear residual away from the center.
3. Critical winding near-sharpness therefore requires continuous dynamic maintenance by external/network modes.

## Next frontier — null-canceling transaction network

The surviving singular mechanism now needs to do three jobs simultaneously:

1. amplify the core;
2. provide the pure Gram shape-servo channel;
3. cancel/export the nonlinear transaction-null modes generated by the sharp carrier itself.

The next network microscope should ask whether a finite closed set of productive carriers can satisfy all three response constraints recursively, or whether every attempted cancellation generates another required mode outside the finite network.
