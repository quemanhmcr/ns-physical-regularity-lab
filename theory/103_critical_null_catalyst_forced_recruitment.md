# Forced source-relative recruitment in the critical null-catalyst branch

The critical localized catalyst has

`Gamma(tau)~tau^(-beta/2)`,

`L(tau)~tau^beta`,

with `beta=(1+4alpha)/7<1/2`.

Therefore the active peak-cap circulation must grow at rate

`Gamma_dot=(beta/2) Gamma/tau`.

Now use the exact ancestry ledger channel by channel.

The catalyst self-Euler conversion contributes zero cap-flux change.

The moving shrinking cap contributes

`Gamma_dot_shrink=-3 beta Gamma/tau`.

For the `p=2` finite-energy turnover collar, Kelvin gives

`Gamma_dot_nu=-(77/2) nu Gamma/L^2`.

Hence the remaining source-relative material recruitment must be

`Gamma_dot_recruit = [7beta/2 +(77/2)nu tau/L^2] Gamma/tau`.

Since `nu tau/L^2 ->0`,

`Gamma_dot_recruit ~ (7beta/2) Gamma/tau`.

The net active flux grows only at `(beta/2)Gamma/tau`; recruitment is asymptotically seven times that rate because most recruited flux is lost from the moving active cap by geometric shrinkage.

Dropping the negative Kelvin term gives the branchwise lower bound

`integral Gamma_dot_recruit dt >= 7[Gamma(t)-Gamma(t0)]`.

Since `Gamma(t)->infinity`, the required cumulative source-relative circulation crossing diverges.

This is not an event-count observer and not a norm. It is the exact circulation ancestry current required by the moving source.

The unresolved escape is reuse: the same frozen material ancestry could cross the active source many times. A contradiction now requires a theorem about **recrossing multiplicity / material genealogy**, not about energy, enstrophy, or the amount of circulation in one instantaneous snapshot.
