# Stationary productive amplification: the dangerous branch does not need shape recycling

## THINK — a singular mechanism may sit near the best shape

The fixed-scale reset trilemma attacks repeated Gram-shape excursions.  But a more dangerous strategy is available: keep a productive pair near a highly efficient Gram geometry and amplify vorticity magnitudes without moving the shape.

The pair-generated strain frame lets us pose this branch exactly.

At the global capacity geometry

`alpha=1/sqrt(3)`,

`beta=-1/sqrt(3)`,

`T=2/3`,

ask for a common symmetric strain with response vector

`(sigma_a,sigma_b,alphadot,betadot,gammadot)`

`=(sigma,sigma,0,0,0)`.

Because the pair strain frame is full rank, there is a unique STF strain with these five responses.

## Exact stationary amplifier

The strain is

`S_* = sigma [[1,0,3/(2sqrt(2))],`

`             [0,1,3/(2sqrt(2))],`

`             [3/(2sqrt(2)),3/(2sqrt(2)),-2]]`.

It satisfies

`a.S_* a=sigma`,

`b.S_* b=sigma`,

while

`M_alpha:S_*=M_beta:S_*=M_gamma:S_*=0`.

Thus both lineages amplify at the same positive rate while the complete Gram shape stays stationary to first order.

This kills any proposed regularity mechanism requiring infinite productive shape variation as a necessary precursor to large vorticity amplification.

## Sharp harmonic occupancy

The exact squared strain size is

`|S_*|^2=(21/2)sigma^2`.

A pure harmonic linear realization saturates the Hodge energy floor, so on radius `r`

`E_h(B_r)`

`=(2pi/15)|S_*|^2 r^5`

`=(7pi/5)sigma^2 r^5`.

Consequently finite kinetic energy `E0` gives the exact fixed-scale harmonic ceiling

`sigma <= sqrt(5 E0/(7pi)) r^(-5/2)`.

Therefore stationary optimal amplification cannot be driven at unbounded rate by a harmonic source on a physical radius bounded away from zero.

The surviving routes are the same physical alternatives exposed by the reset microscope:

- move the harmonic support scale inward;
- obtain the production through the local vortical Hodge transaction;
- obtain it through endpoint/bridge/viscous residual ancestry sources.

## Complete five-channel Hodge transaction frame

For a noncoplanar pair, the five pair-generated STF tensors are

`A_a, A_b, M_alpha, M_beta, M_gamma`.

Since they form a basis of `STF(3)`, every Hodge shell tensor `Q(rho)` is completely determined by the five physical projections

`A_a:Q`, `A_b:Q`, `M_alpha:Q`, `M_beta:Q`, `M_gamma:Q`.

Moreover every one of these channels obeys the same Hodge screen:

`E_i:S_v(r)`

`=integral_0^r [1-(rho/r)^5] [E_i:Q(rho)] d rho/rho`.

The first two channels change vorticity magnitude.  The last three change productive pair shape.  They are not separate analytical theories; they are the five physical coordinates of one screened strain transaction.

## PROMOTE

1. Large vorticity amplification does not require Gram-shape recycling; a pair can remain at the optimal productive shape while both lineages amplify.
2. The optimal equal stationary amplifier has exact harmonic occupancy `(7pi/5)sigma^2 r^5`.
3. At fixed positive Hodge scale and finite kinetic energy, its harmonic amplification rate is uniformly bounded.
4. Magnitude production and shape renewal are jointly resolved by one complete pair-generated Hodge transaction frame.

## Next frontier — persistent production transaction ancestry

The remaining hard branch is now very specific.  A singular candidate may keep shape currents small, keep the pair strongly noncoplanar, and repeatedly source large positive magnitude production through the vortical/residual channels at smaller scales or through new material lineages.

The next problem is therefore not another spatial norm estimate.  It is to follow the **material ancestry of the five-channel pair transaction frame** and determine whether a finite stock of circulation/ancestry can repeatedly supply the two positive production channels while keeping the three shape channels inside the productive chamber.

This is where the Hodge transaction microscope, pair ancestry cell, and material-spacetime viscous current finally meet in one observer.
