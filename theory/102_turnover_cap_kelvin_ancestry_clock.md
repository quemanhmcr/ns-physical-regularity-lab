# Kelvin ancestry clock in the turnover collar

Localize the null catalyst by

`u=chi(r/L)u0`.

The angular velocity field `u0` is a harmonic homogeneous polynomial of degree two. Therefore exactly

`Delta(chi u0)=[chi''+6 chi'/r]u0`.

The catalyst flow keeps each centered spherical cap material. Kelvin's law then gives the circulation mutation of its boundary loop directly:

`Gamma_dot/Gamma = (nu/L^2) [chi_ss+6 chi_s/s]/chi`.

For the physical taper

`chi=(1-s^2)^p`,

the radial cap flux `s^3 chi` peaks at

`s^2=3/(2p+3)`.

At that peak,

`Gamma_dot/Gamma = -[8p+18+9/p] nu/L^2`.

Thus the same turnover collar that carries all localized enstrophy is also the place where material circulation ancestry is actually destroyed by viscosity.

This closes a causal loop:

- interior linear null germ: Euler-reusable, viscosity-null;
- self-Euler conversion: productive transaction, no circulation mutation;
- moving source: changes inventory by recruitment/crossing;
- finite-energy turnover: introduces Kelvin ancestry decay on the halo viscous clock.

In the critical scaling that halo clock exceeds the remaining time, so the required growing circulation inventory still cannot be supplied by turnover viscosity. Positive growth must be routed through external material recruitment/deformation.
